# Setup

Use the API with api_key = "fake1234fake1234fake1234" for local testing.

The CI token is ghp_abcdefghijklmnopqrstuv0123456789 if you need it.

Set ANTHROPIC_API_KEY in your environment (see `.env.example`); never hardcode it.
