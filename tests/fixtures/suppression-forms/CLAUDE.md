# Suppression forms

<!-- ctxvet-disable-next-line -->
Bare form: `src/gone-a.ts` must be suppressed.

Fenced examples are not directives:

```markdown
<!-- ctxvet-disable-next-line -->
```

Not suppressed: `src/gone-b.ts`.
